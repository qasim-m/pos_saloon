<!DOCTYPE html>
<html>
  <head>
    <title>Najim Aldroob</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
 
  </head>
  <body>
  <nav class="navbar navbar-expand-lg bg-dark bg-gradient">
    <div class="container-fluid">
      <a class="navbar-brand text-white align-self-start h-100 fs-5 fw-bold">Najim Aldroob - Gents Saloon</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse mx-2 align-items-center" id="navbarSupportedContent">
        <button type="button" class="btn btn-outline-success btn-light d-flex mx-1 btn-outline-info fw-bold"><%= link_to "Home", root_path %></button>
        <button type="button" class="btn btn-light d-flex mx-1 btn-outline-info fw-bold"><%= link_to "Your Sales", sales_path %></button>
        <% if current_user && current_user.admin? %>
          <button type="button" class="btn btn-light d-flex mx-1 btn-outline-info fw-bold"><%= link_to 'Manage Services', services_path%></button>
        <% end %>
      </div>
        <button type="button" class="btn btn-light btn-outline-danger d-flex mx-1 fs-6 fw-bold"><%= link_to 'Sign Out', destroy_user_session_path, method: :delete%></button>
    </div>
  </nav>
    <% flash.each do |name, msg| %>
      <%= msg %>
    <% end %>
    <br>
    <%= yield %>
  </body>
</html>
